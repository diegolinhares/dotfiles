## Coding conventions

- Name classes, modules, and methods by what they do in the business domain, not by which technical pattern they implement. `PlaceOrder` not `OrderFacade`, `UnifiedPrinter` not `PrinterAdapter`, `Draft` not `DraftState`. The code should read like the business, not like a textbook.
